services:
  - type: web
    name: crisisassist-backend
    env: node
    plan: free
    buildCommand: npm install && cd backend && npm install && npx tsc
    startCommand: cd backend && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
